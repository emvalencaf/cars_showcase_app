/// components
import Navbar from "../Navbar";

const Header = () => {
    return (
        <header className="w-full absollute z-10">
            <Navbar />
        </header>
    )
};

export default Header;